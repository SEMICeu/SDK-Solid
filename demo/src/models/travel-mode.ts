/**
 * List of supported travel modes in the demo.
 */
export const travelModes = [ 'Bus', 'Train', 'Car' ];

/**
 * Represents a mode of travel.
 */
export type TravelMode = typeof travelModes[number];
